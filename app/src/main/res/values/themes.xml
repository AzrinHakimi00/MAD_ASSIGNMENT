<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MAD_ASSIGNMENT" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.NavGraph" parent="Theme.MaterialComponents.DayNight.NoActionBar"/>
    <style name="Theme.MAD_ASSIGNMENT" parent="Base.Theme.MAD_ASSIGNMENT" />

    <style name="NeonProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/neon_progress_bar</item>
    </style>

    <style name="CustomSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:background">#ffffff</item>
        <item name="android:popupBackground">#3C3C3C</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textColorHint">#000000</item>
        <item name="android:dropDownVerticalOffset">10dp</item> <!-- Adjust vertical offset as needed -->
        <item name="android:dropDownHorizontalOffset">10dp</item> <!-- Adjust horizontal offset as needed -->
        <!-- Add other styling attributes as needed -->
    </style>

</resources>